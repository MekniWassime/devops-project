apiVersion: apps/v1
kind: Deployment
metadata:
  prometheus.io/port: "8000"
  prometheus.io/path: "/metrics"
  prometheus.io/scrape: "true"
  name: payment-deployment
  labels:
    app: payment
  namespace: payment-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment
  template:
    metadata:
      labels:
        app: payment
    spec:
      containers:
      - name: payment
        image: mekniwassime/devops_project:payment-c61050728b2a55573adcecf72a268b8286327d80
        ports:
        - containerPort: 80